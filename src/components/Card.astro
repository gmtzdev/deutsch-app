<section class="card">
    <slot />
</section>

<style>
    .card {
        width: min(850px, 100%);
        background: #fff;
        border-radius: 14px;
        padding: 1.5rem;
        box-shadow: 0 8px 28px rgb(0 0 0 / 8%);
    }
</style>
