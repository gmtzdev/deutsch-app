---
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import ActivitesListA12 from "@/components/ActivitesListA12.astro";
import Card from "@/components/Card.astro";
import TagGrammatik from "@/components/Tags/TagGrammatik.astro";
import TagWortschatz from "@/components/Tags/TagWortschatz.astro";
import "@/styles/page.css";
import "@/styles/tables.css";
---

<Layout>
    <Header />

    <div class="grid grid-cols-1 md:grid-cols-[300px_1fr]">
        <ActivitesListA12 open={4} />

        <main class="page bg-background">
            <Card>
                <h1>Rollenspiel</h1>
                <p class="subtitle text-indications">Sprechen Sie zusammen.</p>

                <div class="sections">
                    <div class="section">
                        <ul class="obj-list text-text">
                            <li>
                                <span class="dot dot-blue"></span>
                                Über die letzte Reise sprechen
                                <em></em>
                            </li>
                            <li>
                                <span class="dot dot-blue"></span>
                                Was hast du mit genommen?
                                <em></em>
                            </li>
                            <li>
                                <span class="dot dot-blue"></span>
                                Wie lange warst du im Urlaub?
                            </li>
                            <li>
                                <span class="dot dot-blue"></span>
                                Was sollst du besser machen?
                            </li>
                        </ul>

                        <p>Stelle drei weitere Fragen.</p>
                    </div>
                </div>
            </Card>
        </main>
    </div>

    <style>
        .obj-list {
            list-style: none;
            padding: 0;
            margin: 0;
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .obj-list li {
            display: flex;
            align-items: center;
            gap: 0.55rem;
            font-size: 0.95rem;
        }

        .dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            flex-shrink: 0;
        }

        .dot-blue {
            background: #3b82f6;
        }

        .vocab-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1.5rem;
        }

        .vocab-group h4 {
            font-size: 0.82rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            color: #6b7280;
            margin: 0 0 0.5rem 0;
        }

        @media (max-width: 600px) {
            .vocab-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</Layout>
